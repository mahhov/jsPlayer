{
  "name": "js-player",
  "version": "1.0.0",
  "description": "electron app to play local media",
  "main": "src/electronStarter.js",
  "scripts": {
    "start": "electron .",
    "package": " rm -rf package; mkdir package; cp -r src package; cp -r node_modules/font-awesome/fonts package/fonts; cp package.json package; cd package; npm i; electron-packager ."
  },
  "author": "mahhov1",
  "license": "ISC",
  "devDependencies": {
    "electron": "^4.1.1",
    "electron-packager": "^13.1.0"
  },
  "dependencies": {
    "dwytpl": "^3.3.0",
    "env-paths": "^2.1.0",
    "font-awesome": "^4.7.0"
  }
}
